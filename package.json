{
  "name": "mittiguard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env server.mjs",
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node scripts/evaluate.mjs && node scripts/store-test.mjs && node scripts/model-guard-test.mjs",
    "check": "node --check server.mjs && node --check public/app.js && npm test",
    "smoke:model": "node --env-file-if-exists=.env scripts/live-model-smoke.mjs",
    "smoke:gpt": "MODEL_PROVIDER=openai node --env-file-if-exists=.env scripts/live-model-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
